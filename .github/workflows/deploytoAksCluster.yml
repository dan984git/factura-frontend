on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    
    - uses: azure/docker-login@v1
      with:
        login-server: myaksproyectofinal.azurecr.io
        username: ${{ secrets.acr_myaksproyectofinal_username }}
        password: ${{ secrets.acr_myaksproyectofinal_password }}
    
    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/" -f  "Dockerfile" -t myaksproyectofinal.azurecr.io/myakscluster:${{ github.sha }} --label dockerfile-path=Dockerfile
        docker push myaksproyectofinal.azurecr.io/myakscluster:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      with:
         kubeconfig: ${{ secrets.aks_myakscluster_kubeConfig }}
      id: login
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep myaksclustera9f7 | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace myaksclustera9f7`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: myaksclustera9f7
        container-registry-url: myaksproyectofinal.azurecr.io
        container-registry-username: ${{ secrets.acr_myaksproyectofinal_username }}
        container-registry-password: ${{ secrets.acr_myaksproyectofinal_password }}
        secret-name: myaksclusterdockerauth
       
    - uses: azure/k8s-deploy@v1.2
      with:
        namespace: myaksclustera9f7
        manifests: |
          manifests/deployment-1.yml
          manifests/service-1.yml
          manifests/ingress.yml
        images: |
          myaksproyectofinal.azurecr.io/myakscluster:${{ github.sha }}
        imagepullsecrets: |
          myaksclusterdockerauth
