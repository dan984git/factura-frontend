apiVersion: v1
kind: Service
metadata:
    name: "myakscluster-b87b"
    labels:
        app: "myakscluster-b87b"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "myakscluster-b87b"